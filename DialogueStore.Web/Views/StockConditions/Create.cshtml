@model DialogueStore.Web.Models.StockCondition

@{
    ViewBag.Title = "Create Stock Condition";
}

<div class="container">
    <div class="row">
        <h2 class="page-header">Create Stock Condition</h2>
    </div>

    <div class="row">
        <ul class="nav nav-pills">
            <li><a href="@Url.Action("Index")">List</a></li>
            <li class="active"><a href="@Url.Action("Create")">Create New</a></li>
        </ul>
    </div>

    <p>&nbsp;</p>

    <div class="row">

        <div class="col-md-6">
            @using (Html.BeginForm()) {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)

                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3>Stock Condition Details</h3>
                    </div>
                    <div class="panel-body">
                        <div class="form-group">
                            @Html.LabelFor(f => f.Name)
                            <div>
                                @Html.EditorFor(f => f.Name)
                                @Html.ValidationMessageFor(f => f.Name)
                            </div>
                        </div>

                        <div class="form-group">
                            <input type="submit" value="Create" class="btn btn-primary" />
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
